- 阶段0 : 确认Jazzy / colcon / workspace 环境
- 阶段1: 创建自己的ROS2 + BehaviorTree.CPP 工程
- 阶段2: 写自定义BT节点: Condition / Action / StatefulAction
- 阶段3: 把行为树接入ROS2 Topic /Blackboard
- 阶段4: 理解ROS2 Action, 准备接Nav2 
- 阶段5: 运行Nav2 仿真, 查看默认BT XML
- 阶段6: 修改Nav2 行为树 XML
- 阶段7: 写Nav2 自定义BT插件

ROS2的包通常用ament作为构建系统、colcon作为构建工具; 一个CMake类型ROS2包通常包含
CMakeLists.txt、package.xml、include/、src/等结构

### 阶段0 : 环境确认
打开终端, 先执行
```
source /opt/ros/jazzy/setup.bash
echo $ROS_DISTRO
ros2 pkg list | head
```